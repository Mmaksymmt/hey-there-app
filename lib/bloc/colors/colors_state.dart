import 'package:flutter/material.dart';

class ColorsState {
  final String text;
  final List<Color> colorsHistory;
  final Color color;
  final List<Color> textColors;

  ColorsState(this.text, this.colorsHistory, this.color, this.textColors);
}
